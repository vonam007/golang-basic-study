package ifelse

// Classify trả về "positive", "negative", hoặc "zero" tùy theo giá trị n.
func Classify(n int) string {
	// TODO: Implement this
	return ""
}

// Grade trả về xếp loại dựa trên điểm (0-100).
// score >= 90 → "A"
// score >= 80 → "B"
// score >= 70 → "C"
// score >= 60 → "D"
// score < 60  → "F"
// score < 0 hoặc > 100 → "Invalid"
func Grade(score int) string {
	// TODO: Implement this
	return ""
}

// FizzBuzz trả về:
// "FizzBuzz" nếu n chia hết cho cả 3 và 5
// "Fizz" nếu n chia hết cho 3
// "Buzz" nếu n chia hết cho 5
// Chuỗi biểu diễn số nếu không chia hết cho 3 hoặc 5
func FizzBuzz(n int) string {
	// TODO: Implement this
	return ""
}

// ClampValue giới hạn value trong khoảng [min, max].
// Nếu value < min → trả min.
// Nếu value > max → trả max.
// Ngược lại → trả value.
func ClampValue(value, min, max int) int {
	// TODO: Implement this
	return 0
}
